# DEMO_CHECKLIST.md — 人に使ってもらう日の手順

Club Activities（反対立論）の最小授業を、別の PC で人に触ってもらうためのメモ。

---

## 前日までに（ここが本番）

- [ ] その PC で `npm run doctor` が**すべて緑**になる
- [ ] **自分で一度、最後まで音声を通しておく**

2つ目が肝心。**この授業は実接続でしか確かめられない部分がある。**
当日その場で初めて試すと、転んだときに直す時間が無い。

一度通すときに見るのはこの3点。

1. AI が `against` の質問を**1問だけ**して**止まる**か（答えを先に言ってしまわないか）
2. こちらが黙っている間、**AI が待てる**か（間を置くと割り込んでこないか）
3. 「反対」と答えたあと、**`optional` の質問へ進む**か（フェーズが進むか）

3 が動かない場合、モデルが `mark_phase_complete` を呼べていない可能性が高い。
そのときは `npm run dev` のログに `[phase]` の行が出ているかを見て、
私に伝えてもらえれば instructions か tool の説明文を調整する。

---

## 当日の流れ

```bash
npm run doctor     # 念のため
npm run dev
```

1. ブラウザで `http://localhost:3000/student` を開く
2. 「AI授業を開始」
3. 授業画面の「開始」を押す
4. **マイクの利用を許可する**（ここで止まる人が多い）
5. AI が日本語で話しかけてくる。声で答える

---

## 相手に先に伝えておくこと

- **30〜40分の授業**。読解 → 論理構造の確認 → 自分の理由づくり → 英語化 → AIとのミニディベート
- **途中でやめてよい。** フェーズは DB に保存されるので、次に開けば続きから戻る
- 答えは**日本語でよい**
- **点数は出ない**（採点は音声セッション中に行わない設計のため）
- **音声は保存しない。書き起こしは保存する**

---

## 転んだときの一次対応

| 症状 | 見るところ |
|---|---|
| 画面は出るが AI が話さない | マイク権限。ブラウザのアドレスバーの左側 |
| 「開始」でエラー | `npm run doctor`。キーとモデル名が有効か |
| AI が答えを先に言う | instructions のルールが効いていない。ログを控えて共有 |
| `optional` へ進まない | `mark_phase_complete` が呼ばれていない。同上 |
| AI が先の質問を勝手に始める | instructions が長い（約1万5千字）ため。教材 JSON の `lesson_phases` を減らせばすぐ短くできる |
| 途中で切れた | もう一度「開始」。フェーズは DB に保存されているので続きから戻る |

`npm run dev` を動かしているターミナルのログが一番の手がかり。
**エラーが出たらその行を残しておいてもらえると、原因を特定しやすい。**

---

## 分かっている弱点

- **instructions が約1万5千字ある。** 13フェーズ分の質問・受理する答え・ヒントを
  接続時にまとめて渡しているため。`docs/REALTIME_ARCHITECTURE.md` §3 が言う
  「長い prompt は指示の効きが落ちる」領域に入っている。
  現在のフェーズを prompt の末尾側に置いて対策しているが、**実接続で確かめていない。**
  もし AI が先へ進んでしまうようなら、教材 JSON からフェーズを減らすのが一番速い
- **語彙の意味は左ペインに出ている。** `mandatory participation = 強制参加` のように、
  語彙を問う質問は画面を見れば答えが分かる。v03 §5 が語彙表示を求めているための
  トレードオフで、隠すなら別途相談

## 終わったあと

- [ ] **この用途で発行した API キーを失効させる**（別の PC に残るため）
- [ ] `psql $DATABASE_URL -c "select speaker, text from session_transcript order by seq"`
      で会話の記録を見る。うまくいかなかった箇所の手がかりになる
